# specs/

One file per non-trivial feature: `specs/<feature>.md`, capturing the **what** and **why** — not implementation detail. The spec is the source artifact; code is generated from it.

Write a spec before planning/building a feature (see [../docs/WORKFLOW.md](../docs/WORKFLOW.md)). Small contained edits skip this.
