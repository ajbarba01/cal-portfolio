/**
 * Socials config — drop Cal's profile URLs here when they land.
 * An empty string means the icon is NOT rendered (no dead `#` links).
 * The mail entry always renders and always points to /contact (never mailto:).
 */
export const socials = {
  /** Instagram profile URL — leave empty until Cal supplies one. */
  instagram: "" as string,
  /** TikTok profile URL — leave empty until Cal supplies one. */
  tiktok: "" as string,
} as const;
