/**
 * Section layout tokens shared by the client-detail page sections. Booking
 * status text and badge variants come from `bookingStatusPill`, the one map
 * every booking surface reads.
 */

// Layout-only — the surface (bg/border/radius/shimmer) comes from Surface emphasis.
export const SECTION = "flex flex-col gap-3 p-4";
export const LEGEND =
  "text-brand-strong text-xs font-semibold tracking-wide uppercase";
