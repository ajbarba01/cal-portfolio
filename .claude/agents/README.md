# .claude/agents/

Project subagent definitions. Use subagents for read-heavy exploration or isolated, specialized tasks; they run in their own context and return a condensed summary, keeping the main context lean.

Add agents here as needs emerge (e.g. an explorer for codebase search, a reviewer for diffs). None defined yet.
