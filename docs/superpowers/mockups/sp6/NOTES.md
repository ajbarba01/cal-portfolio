# SP6 mockups — sign-off record

Token-accurate HTML previews (real Trail palette + Fraunces/Public Sans). Each signed off by the maintainer one at a time, 2026-06-11, **before any build**. These are the visual contract for SP6 execution; deviations get logged in the plan Handoff log.

| Preview                                                         | Surface                                               | Verdict + load-bearing notes                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [contact-preview.html](previews/contact-preview.html)           | /contact                                              | Approved after revision: **no public email/phone** — the form is the channel (footer mail icon → /contact, never mailto); fields **stacked single-column** (Baymard) not 2×2. Establishes the sitewide form-on-card recipe + inline success state.                                                                                           |
| [booking-flow-preview.html](previews/booking-flow-preview.html) | /book/[service] + admin create/edit (`<BookingFlow>`) | Approved after revision: **stacked single column on all viewports — no sticky side receipt**; summary card inline above CTA. **Calendar keeps its current in-app visual** (maintainer likes it) — only U2 grey lead-time, premium dot + legend, quiet contact note are added. U1 success panel + U6 settings-rendered policy line as mocked. |
| [home-chrome-preview.html](previews/home-chrome-preview.html)   | Home trust band + header + footer                     | Approved on the **real registry copy** version (long-form bodies, left-aligned, icon+title row; maintainer asked to see actual text before signing). Header 60px + tab hover/underline; burger optically flush right; footer aligned to header width; socials hidden until Cal supplies URLs. why-Cal header stays a copy-sync placeholder.  |
| [system-preview.html](previews/system-preview.html)             | Plan A conventions                                    | Approved. **Badge color: slate `#3c5566`** (option A; gold rejected, clay/pine declined). Select `--anchor-width`, button hierarchy (near-black retires from forms), zone skeletons + SiteHeader-Suspense catch, toast typing, confirm pattern all locked.                                                                                   |

---

_Last reviewed: 2026-06-11_
